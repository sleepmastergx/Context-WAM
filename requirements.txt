# Known-good pins from the development environment (python 3.10).
# torch must match the cluster's CUDA — 2.7.1+cu128 was used here; install it
# per https://pytorch.org before the rest if pip resolves a different build.
torch==2.7.1
accelerate==1.12.0
deepspeed==0.18.5
transformers==4.49.0
huggingface-hub<1.0
diffusers
omegaconf
hydra-core
numpy
pyyaml
einops
safetensors
wandb
